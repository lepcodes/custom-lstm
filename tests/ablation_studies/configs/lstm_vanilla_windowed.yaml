# ─── Ablation Study: LSTM Vanilla Windowed ──────────────────────────────────
# Usage: python train.py --config configs/lstm_vanilla_windowed.yaml

experiment_name: "Thesis_Ablation"
data_path: "C:\\Users\\Luis\\Documents\\ML-AI-Projects\\custom-lstm\\data\\preprocessed\\lbl_tcp_3.csv"

architecture: "lstm_vanilla_windowed"
data_mode: "win"

model_kwargs:
  input_size: 30
  hidden_size: 64
  output_size: 1

window_size: 30
epochs: 100
lr: 0.001
bptt_steps: 100
