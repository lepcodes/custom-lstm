# ─── Sweep W-0: Custom LSTM Windowed + MSE Only (Control) ──────────────────
# Usage: python sweep.py --config search/phase_2/sweep_W0_windowed_mse.yaml

data_path: "C:\\Users\\Luis\\Documents\\ML-AI-Projects\\custom-lstm\\data\\preprocessed\\ethernet_traffic.csv"
architecture: "lstm_custom_windowed"
data_mode: "win"
loss_type: "mse"

# Fixed hyperparameters
output_size: 1
epochs: 200
patience: 30

# Search grid
grid:
  hidden_size: [32]
  window_size: [10]
  bptt_steps: [50]
  lr: [0.001]
