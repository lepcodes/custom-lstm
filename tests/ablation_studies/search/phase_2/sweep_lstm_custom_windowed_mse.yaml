# ─── Sweep W-0: Custom LSTM Windowed + MSE (Control) ───────────────────────
# Wide Search: Comparing Windowed capacity vs Pure baseline.

data_path: "C:\\Users\\Luis\\Documents\\ML-AI-Projects\\custom-lstm\\data\\preprocessed\\ethernet_traffic.csv"
experiment_name: "Phase 2"
architecture: "lstm_custom_windowed"
data_mode: "win"
loss_type: "mse"

# Fixed hyperparameters
output_size: 1
epochs: 200
patience: 30

# Search grid
grid:
  hidden_size: [32, 64, 128]
  lr: [0.001, 0.0001]
  bptt_steps: [50, 100]
  window_size: [5, 10, 20]
