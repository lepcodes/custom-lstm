# ─── Sweep P-0: Custom LSTM Pure + MSE Only (Control) ──────────────────────
# Wide Search: Evaluating core capacity and learning dynamics.

data_path: "C:\\Users\\Luis\\Documents\\ML-AI-Projects\\custom-lstm\\data\\preprocessed\\ethernet_traffic.csv"
experiment_name: "Phase 2"
architecture: "lstm_custom_pure"
data_mode: "pure"
loss_type: "mse"

# Fixed hyperparameters
output_size: 1
epochs: 200
patience: 30

# Search grid
grid:
  hidden_size: [32, 64, 128]
  lr: [0.001, 0.0001]
  bptt_steps: [50, 100]
