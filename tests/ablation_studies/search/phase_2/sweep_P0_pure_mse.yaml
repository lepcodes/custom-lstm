# ─── Sweep P-0: Custom LSTM Pure + MSE Only (Control) ──────────────────────
# Usage: python sweep.py --config search/phase_2/sweep_P0_pure_mse.yaml

data_path: "C:\\Users\\Luis\\Documents\\ML-AI-Projects\\custom-lstm\\data\\preprocessed\\ethernet_traffic.csv"
architecture: "lstm_custom_pure"
data_mode: "pure"
loss_type: "mse"

# Fixed hyperparameters
output_size: 1
epochs: 200
patience: 30

# Search grid
grid:
  hidden_size: [32]
  bptt_steps: [50]
  lr: [0.001]
